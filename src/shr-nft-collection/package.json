{
  "name": "shr-nft-collection",
  "description": "SHR NFT Subgraph",
  "version": "1.0.0",
  "author": "SHR",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ shr/nft-collection subgraph.yaml"
  }
}
